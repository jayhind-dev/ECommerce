@using ECommerceStore.Models;
@model ECommerceStore.Models.VMProduct
<style>

    p {
        margin: 0;
    }

    .upload__box {
        padding: 40px;
    }

    .upload__inputfile {
        width: 0.1px;
        height: 0.1px;
        opacity: 0;
        overflow: hidden;
        position: absolute;
        z-index: -1;
    }

    .upload__btn {
        display: inline-block;
        font-weight: 600;
        color: black;
        text-align: center;
        min-width: 116px;
        padding: 5px;
        transition: all 0.3s ease;
        cursor: pointer;
        border: 2px solid;
        background-color: #f4f2f0;
        border-color:black;
        border-radius: 10px;
        line-height: 26px;
        font-size: 14px;
    }

    .upload__btn:hover {
            background-color: unset;
            color: #4045ba;
            transition: all 0.3s ease;
        }

    .upload__btn-box {
        margin-bottom: 10px;
    }

    .upload__img-wrap {
        display: flex;
        flex-wrap: wrap;
        margin: 0 -10px;
    }

    .upload__img-box {
        width: 70px;
        padding: 0 10px;
        margin-bottom: 12px;
    }

    .upload__img-close {
        width: 24px;
        height: 24px;
        border-radius: 50%;
        background-color: rgba(0, 0, 0, 0.5);
        position: absolute;
        top: 10px;
        right: 10px;
        text-align: center;
        line-height: 24px;
        z-index: 1;
        cursor: pointer;
    }

    .upload__img-close:after {
            content: "✖";
            font-size: 14px;
            color: white;
    }

    .img-bg {
        background-repeat: no-repeat;
        background-position: center;
        background-size: cover;
        position: relative;
        padding-bottom: 100%;
    }

    .box-img01 {
        border: 2px dotted #5c40ba4d;
        background-color: ;
    }
</style>
<div class="col-md-8">
    <div class="form-group">
        <label>main_image</label>
        <input type="file" id="mailimg" class="form-control file-upload-info " name="Singleimgstring" />
       

    </div>
</div>
<div class="col-sm-12 ">


    <div class="upload__box box-img01">
        <div class="row">
            <div class="col-sm-4">
                <div class="upload__btn-box">
                    <label class="upload__btn">
                        <b> product images</b>
                        <input type="file" multiple="" data-max_length="20" id="multiimg" class="upload__inputfile" name="multiimg">
                        @Html.EditorFor(model => model.Multiimgstring, new { htmlattributes = new { @class = "upload__inputfile", id = "multiimg", placeholder = "upload image", } })
                        @Html.ValidationMessageFor(model => model.Multiimgstring, "", new { @class = "text-danger" })
                    </label>
                </div>
            </div>
            <div class="col-sm-8">
                <div class="upload__img-wrap"></div>
            </div>
        </div>
    </div>
</div>



<script src="~/scripts/jquery-3.4.1.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
<script>
    //jquery(document).ready(function () {
    //    imgupload();
    //});
    //$(document).ready(function () {
    //    imgupload();
    //})

    //function imgupload() {
    //    var imgwrap = "";
    //    var imgarray = [];
    //    debugger;
    //    $('.upload__inputfile').each(function () {
    //        $(this).on('change', function (e) {
    //            imgwrap = $(this).closest('.upload__box').find('.upload__img-wrap');
    //            var maxlength = $(this).attr('data-max_length');
    //            debugger;
    //            var files = e.target.files;
    //            var filesarr = Array.prototype.slice.call(files);
    //            var iterator = 0;
    //            filesarr.foreach(function (f, index) {

    //                if (!f.type.match('image.*')) {
    //                    return;
    //                }
    //                debugger;

    //                if (imgarray.length > maxlength) {
    //                    return false
    //                } else {
    //                    var len = 0;
    //                    for (var i = 0; i < imgarray.length; i++) {
    //                        if (imgarray[i] !== undefined) {
    //                            len++;
    //                        }
    //                    }
    //                    debugger;
    //                    if (len > maxlength) {
    //                        return false;
    //                    } else {
    //                        imgarray.push(f);

    //                        var reader = new filereader();
    //                        reader.onload = function (e) {
    //                            var html = "<div class='upload__img-box'><div style='background-image: url(" + e.target.result + ")' data-number='" + $(".upload__img-close").length + "' data-file='" + f.name + "' class='img-bg'><div class='upload__img-close'></div></div></div>";
    //                            imgwrap.append(html);
    //                            iterator++;
    //                        }
    //                        reader.readAsDataURL(f);
    //                        debugger;
    //                    }
    //                }
    //            });
    //        });
    //    });
    //    debugger;
    //    $('body').on('click', ".upload__img-close", function (e) {
    //        var file = $(this).parent().data("file");
    //        for (var i = 0; i < imgarray.length; i++) {
    //            if (imgarray[i].name === file) {
    //                imgarray.splice(i, 1);
    //                break;
    //            }
    //        }
    //        $(this).parent().remove();
    //    });
    //}
</script>
